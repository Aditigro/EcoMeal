{% extends 'base.html.twig' %}

{% block title %}Hello ConsumerController!{% endblock %}

{% block body %}
    <style>
        .example-wrapper {
            margin: 1em auto;
            max-width: 800px;
            width: 95%;
            font: 18px/1.5 sans-serif;
        }

        .example-wrapper code {
            background: #F5F5F5;
            padding: 2px 6px;
        }
    </style>

    <div class="example-wrapper">
        <h1 class="text-center">Consumer</h1>
        <a class="btn btn-primary position-absolute start-50 translate-middle-x" href="{{ path('app_consumer_new') }}"> Create New Consumer</a>
        <br>
        <br>
        <div class="d-flex justify-content-center">
            <table class="table table-striped">
                <thead>
                <tr class="table-primary">
                    <th class="text-center px-3">Actions</th>
                    <th class="text-center px-3">Id</th>
                    <th class="text-center px-3">First Name</th>
                    <th class="text-center px-3">Last Name</th>
                    <th class="text-center px-3">Phone Number</th>
                </tr>
                </thead>
                <tbody>
                {% for consumer in consumers %}
                    <tr class="table-secondary">
                        <td class="align-middle px-3">
                            <form action="{{ path('app_consumer_delete',{
                                "id": consumer.id}
                            ) }}" method="post">
                                <div class="btn-group">
                                    <a class="btn btn-primary" href="{{ path("app_consumer_update", {
                                        "id": consumer.id}) }}">
                                        Update
                                    </a>
                                    <a class="btn btn-secondary" href="{{ path("app_consumer_view", {
                                        "id": consumer.id}) }}">
                                        View
                                    </a>
                                    <button class="btn btn-danger" type="submit">Delete</button>
                                </div>
                            </form>
                        </td>
                        <td class="text-center px-3 align-middle">
                            {{ consumer.id }}
                        </td>
                        <td class="text-center px-3 align-middle">
                            {{ consumer.firstName }}
                        </td>
                        <td class="text-center px-3 align-middle">
                            {{ consumer.lastName }}
                        </td>
                        <td class="text-center px-3 align-middle">
                            {{ consumer.phoneNumber }}
                        </td>
                    </tr>
                {% endfor %}
                </tbody>
            </table>
        </div>
    </div>
{% endblock %}
