{% extends 'base.html.twig' %}

{% block title %}Register{% endblock %}

{% block body %}
<div class="d-flex justify-content-center">
    <br>
    <br>
    <div class="text-center">
        <h1>Register</h1>
        <p><em>Looking to <a href="{{ path('app_register_business') }}">register a business</a>?</em></p>
    </div>
</div>
<div class="d-flex justify-content-center">
    <br>
    {{ form_errors(registrationForm) }}

    {{ form_start(registrationForm) }}
        {{ form_row(registrationForm.consumer.firstName) }}
        {{ form_row(registrationForm.consumer.lastName) }}
        {{ form_row(registrationForm.consumer.phoneNumber) }}
        {{ form_row(registrationForm.email) }}
        {{ form_row(registrationForm.plainPassword, {
            label: 'Password'
        }) }}
        {{ form_row(registrationForm.agreeTerms) }}

        <button type="submit" class="btn btn-primary">Register</button>
    {{ form_end(registrationForm) }}
</div>
{% endblock %}
