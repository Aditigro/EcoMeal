{% extends 'base.html.twig' %}

{% block title %}Hello BusinessTypeController!{% endblock %}

{% block body %}
    <style>
        .example-wrapper {
            margin: 1em auto;
            max-width: 800px;
            width: 95%;
            font: 18px/1.5 sans-serif;
        }

        .example-wrapper code {
            background: #F5F5F5;
            padding: 2px 6px;
        }
    </style>

    <div class="example-wrapper">
        <h1 class="text-center">{{ businessType.name }}</h1>
        <p class="text-center py-1"> Id: {{ businessType.id }}</p>
        <div class="text-center">
            <form action="{{ path('app_business_type_delete',{
                "id": businessType.id}
            ) }}" method="post">
                <div class="btn-group">
                    <a class="btn btn-primary" href="{{ path("app_business_type_update", {
                        "id": businessType.id}) }}">
                        Update
                    </a>
                    <a class="btn btn-secondary" href="{{ path("app_business_type") }}">
                        Back
                    </a>
                    <button class="btn btn-danger" type="submit">Delete</button>
                </div>
            </form>
        </div>
    </div>

{% endblock %}
